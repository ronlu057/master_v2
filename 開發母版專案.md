# 🚀 母版專案開發白皮書

![Vue.js](https://img.shields.io/badge/Vue%203-35495E?style=for-the-badge&logo=vue.js&logoColor=4FC08D)
![Nuxt.js](https://img.shields.io/badge/Nuxt%204-002E3B?style=for-the-badge&logo=nuxt.js&logoColor=00C58E)
![Vite](https://img.shields.io/badge/Vite-B73BFE?style=for-the-badge&logo=vite&logoColor=FFD62E)
![Pinia](https://img.shields.io/badge/Pinia-F6D365?style=for-the-badge&logo=vue.js&logoColor=white)
![Sass](https://img.shields.io/badge/Sass-CC6699?style=for-the-badge&logo=sass&logoColor=white)
![JavaScript](https://img.shields.io/badge/JavaScript-F7DF1E?style=for-the-badge&logo=javascript&logoColor=black)

本文件為**母版專案**的開發指南與規範。此母版以一套程式碼基底，透過設定切換衍生出**五種專案類型**，供開發團隊快速交付不同需求的網站。著手開發前請詳讀本文件。

> 版本：v2.0　|　更新日期：2026-05-22　|　維護單位：AD-Design 技術部
> 🔗 後端 API 規格請參閱：[一般網站白皮書](./一般網站白皮書.md)

---

## 📑 目錄

- [1. 母版概述](#1-母版概述)
- [2. 核心技術棧](#2-核心技術棧)
- [3. 開發環境與啟動](#3-開發環境與啟動)
- [4. 五種專案類型](#4-五種專案類型)
- [5. 專案目錄架構](#5-專案目錄架構)
- [6. 專案類型切換機制](#6-專案類型切換機制)
- [7. API 串接層與 Mock JSON](#7-api-串接層與-mock-json)
- [8. Block 模組系統](#8-block-模組系統)
- [9. 開發與編程規範](#9-開發與編程規範)
- [10. 頁面與模組對照表](#10-頁面與模組對照表)
- [11. Header 版型切換](#11-header-版型切換)

---

## 1. 母版概述

### 1.1 設計目標

一套母版同時具備「快速套版」與「深度客製」兩端的能力。實務上專案需求落在一條光譜上 —— 從幾乎不改的套版站，到整站手刻。本母版用**一個設定值**決定當前專案落在光譜何處，其餘程式碼共用。

### 1.2 共用基底

無論哪種專案類型，以下三層完全共用、不需重寫：

| 層級 | 內容 |
|---|---|
| **API 串接層** | `composables/useApi.js` — 統一處理[一般網站白皮書](./一般網站白皮書.md) §2.1 回應格式 |
| **元件庫** | 頁首/頁尾/選單/Banner/彈跳廣告/Block 模組/新聞卡片… |
| **頁面路由** | 首頁、新聞、關於、聯絡、搜尋、FAQ、服務據點、購物 等 |

差異在於「**首頁與內頁各用多少手刻、多少 block 模組**」，由專案類型決定。

---

## 2. 核心技術棧

> [!WARNING]
> **無 TypeScript 政策**
> 本專案全程使用 **純 JavaScript (ES6+)**，嚴禁引入 `.ts` 檔案或在 `<script>` 使用 `lang="ts"`。
> 參考網路文件複製範例時，務必手動移除所有 TS 類型定義（`: string`、`interface`、`<T>` 等）。

* 🟢 **基礎框架**：[Vue 3](https://vuejs.org/)（Composition API + `<script setup>`）
* ⛰️ **應用框架**：[Nuxt 4](https://nuxt.com/)（SSR、檔案路由、自動引入）
* ⚡ **建置工具**：[Vite](https://vitejs.dev/)（Nuxt 底層引擎）
* 🍍 **狀態管理**：[Pinia](https://pinia.vuejs.org/)（Setup Store 風格）
* 🎨 **CSS 預處理器**：[Sass / SCSS](https://sass-lang.com/)（搭配 CSS 變數做主題切換）
* 🟨 **程式語言**：JavaScript

---

## 3. 開發環境與啟動

### 3.1 環境需求

| 工具 | 版本 |
| :--- | :--- |
| **Node.js** | `v20.x` 以上（建置時實測 `v24`） |
| **套件管理** | `npm` |
| **編輯器** | VS Code + `Vue - Official` 擴充套件（請關閉 TS 報錯） |

### 3.2 指令

```bash
# 安裝相依套件
npm install

# 啟動本地開發伺服器（http://localhost:3000）
npm run dev

# 建立正式環境佈署包
npm run build

# 預覽正式環境
npm run preview
```

---

## 4. 五種專案類型

由 `.env` 的 `NUXT_PUBLIC_PROJECT_TYPE` 決定，五選一：

| 類型 | 設定值 | 首頁 | 內頁 | 購物 | 適用情境 |
|---|---|---|---|---|---|
| **模組件套版** | `module` | block 模組組裝 | block 模組組裝 | ✗ | 標準形象站，後台 cipage 編輯內容 |
| **客製首頁** | `custom-home` | 手刻 + block 模組組裝 | block 模組組裝 | ✗ | 首頁要特殊視覺、內頁走標準模組 |
| **全部客製開發** | `full-custom` | 手刻 + block 模組組裝 | 手刻 | ✗ | 整站皆需獨特設計 |
| **購物類型** | `shop` | 手刻 + block 模組組裝 | block 模組組裝 | ✓ | 電商站，啟用購物車與商品頁 |
| **臨時站** | `temp` | 固定模板 | 隱藏 | ✗ | 「即將開幕」、活動過渡頁、暫代站 |

### 4.1 功能矩陣

每種類型對應三個旗標（定義於 `app.config.js`）：

| 旗標 | 意義 |
|---|---|
| `customHome` | 首頁採手刻版型、並可嵌入 block 模組（`true`）；或整頁 block 模組組裝（`false`） |
| `customPages` | 內頁手刻（`true`）或 block 模組組裝（`false`） |
| `shop` | 是否啟用購物模組 |
| `minimal` | 精簡模式（臨時站）：固定模板、單頁呈現、隱藏完整選單 |

### 4.2 切換方式

```bash
# 編輯 .env
NUXT_PUBLIC_PROJECT_TYPE=shop

# 重啟 dev server
npm run dev
```

切換後 `<html>` 會帶上 `data-project="shop"`，CSS 變數主題同步換色（見 §6、§9.4）。

---

## 5. 專案目錄架構

採 Nuxt 平鋪式結構（程式碼直接置於專案根目錄）：

```text
📦 master_v2/
 ┣ 📂 assets/styles/    🎨 全域樣式（main.scss：RWD 斷點 mixin + CSS 變數主題 + 共用 class）
 ┣ 📂 components/       🧩 共用元件（自動引入）
 ┃ ┣ AppHeader（頁首派發器）/ AppFooter / SiteMenu / PopupAds / LoadingState / NewsCard / PageBanner
 ┃ ┣ headers/Header01〜XX.vue                                              ← Header 多版型
 ┃ ┣ BlockRenderer + BlockBanner / BlockIntro / BlockNews / BlockGeneric   ← Block 模組
 ┃ ┗ HomeModule / HomeCustom / HomeMinimal                                ← 三種首頁版型
 ┣ 📂 composables/      🪝 組合式函式（自動引入）
 ┃ ┣ useApi.js     API 串接層
 ┃ ┣ useProject.js 專案類型與功能旗標
 ┃ ┣ useSite.js    選單 / 公司資料 / 多語字典
 ┃ ┗ useMeta.js    SEO Meta
 ┣ 📂 utils/            🔧 純函式工具（自動引入；無狀態的格式化 / 驗證等）
 ┣ 📂 middleware/       🛡️ 路由中介層（導航前的守衛 / 攔截）
 ┣ 📂 layouts/         版型（default.vue / shop.vue）
 ┣ 📂 pages/           📄 檔案路由頁面
 ┣ 📂 stores/          🍍 Pinia（useUiStore / useCartStore）
 ┣ 📂 server/
 ┃ ┣ 📂 routes/api/[...slug].js   Mock API 派發器
 ┃ ┗ 📂 mock/db.js                Mock 資料
 ┣ 📜 app.vue          入口（掛 NuxtLayout / 設定 data-project 主題）
 ┣ 📜 app.config.js    五種專案類型功能矩陣
 ┣ 📜 error.vue        錯誤頁
 ┣ 📝 nuxt.config.js   Nuxt 全局設定
 ┗ 📝 .env             專案類型 / API 來源設定
```

---

## 6. 專案類型切換機制

三個檔案協同運作：

```
.env (NUXT_PUBLIC_PROJECT_TYPE)
        ↓ 注入 runtimeConfig.public.projectType
nuxt.config.js
        ↓ 讀取
composables/useProject.js  ←─ 對照 app.config.js 的 projectTypes 矩陣
        ↓ 回傳 { type, customHome, customPages, isShop, isMinimal }
頁面 / 元件
```

### 6.1 在頁面/元件中使用

```javascript
const { type, customHome, isShop, isMinimal } = useProject()
```

### 6.2 首頁分流（`pages/index.vue`）

```vue
<template>
  <HomeMinimal v-if="isMinimal" />       <!-- 臨時站 -->
  <HomeCustom  v-else-if="customHome" /> <!-- 客製首頁 / 全客製 / 購物 -->
  <HomeModule  v-else />                 <!-- 模組件套版 -->
</template>
```

新增專案類型：在 `app.config.js` 的 `projectTypes` 加一筆、`.env` 改設定值即可。

---

## 7. API 串接層與 Mock JSON

### 7.1 統一串接層

所有 API 請求一律經 `composables/useApi.js`，它負責解開[一般網站白皮書](./一般網站白皮書.md) §2.1 的回應格式 `{ status, data: { payload, meta } }`，頁面拿到的直接是 `payload`。

```javascript
// SSR 資料獲取（頁面 / 元件 setup 階段）
const { data } = await useApiData('/home/view')

// 命令式請求（表單送出等 POST 互動）
const res = await $api('/contact/submit', { method: 'POST', body })
```

### 7.2 Mock JSON ↔ 真實後端切換

由 `.env` 的 `NUXT_PUBLIC_API_BASE` 控制：

| `NUXT_PUBLIC_API_BASE` | 行為 |
|---|---|
| 留空 | 打本地 mock JSON（`server/routes/api/[...slug].js`） |
| 填後端網域 | 打真實後端，回應格式不變，**前端程式碼無需修改** |

前端開發階段一律留空，用 mock 資料開發版面與互動；後端就緒後再填網域。

### 7.3 Mock 層結構

- `server/mock/db.js` — 模擬資料（對應白皮書各 controller 的 payload）
- `server/routes/api/[...slug].js` — 派發器，依路徑/方法回傳對應 mock，並包成白皮書回應格式

新增 mock endpoint：在 `db.js` 補資料、在派發器 `switch` 加 `case` 即可。

---

## 8. Block 模組系統

Block 系統讓後端一次回完整資料、前端只負責 render（[一般網站白皮書](./一般網站白皮書.md) §3）。模組件套版整頁皆由 block 組裝；客製首頁／全客製／購物則在手刻版型中嵌入 block 模組（見 `components/HomeCustom.vue`）。

### 8.1 資料結構

```
payload.slots[] → slot.blocks[] → block.item.rows[]
```

每個 `block` 帶 `type`（決定套哪個元件）、`title`、`item.rows`（內容固定取此）。

### 8.2 BlockRenderer

`components/BlockRenderer.vue` 維護 `block.type → 元件` 對照表，未知 type 直接 skip：

| `block.type` | 對應元件 |
|---|---|
| `banner` | `BlockBanner`（輪播） |
| `intro` | `BlockIntro`（圖文左右排版） |
| `news` | `BlockNews`（新聞卡片列） |
| `push` / `other_ad` | `BlockGeneric`（通用卡片格） |

新增 block 樣式：建立 `BlockXxx.vue`、在 `BlockRenderer.vue` 的對照表加一筆。

> ⚠️ Nuxt 自動引入的元件**無法**用 `resolveComponent('字串')` 取得；
> Block 元件須在 `BlockRenderer.vue` 以 `import` 明確引入後放入對照表。

---

## 9. 開發與編程規範

### 9.1 Vue 元件規範

1. 統一 `<script setup>` 語法。
2. **不使用 TypeScript**。
3. 元件命名 **PascalCase** 且多字詞（如 `AppHeader.vue`、`NewsCard.vue`）。
4. 善用 Nuxt 自動引入：`components/`、`composables/`、`stores/` 與 Vue API（`ref`、`computed`…）皆免手動 `import`。

### 9.2 狀態管理（Pinia）

- Store 集中於 `stores/`，採 **Setup Store** 風格。
- 範例：`useUiStore`（行動選單開關）、`useCartStore`（購物車）。

```javascript
// stores/useCartStore.js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useCartStore = defineStore('cart', () => {
  const items = ref([])
  const count = computed(() => items.value.reduce((s, i) => s + i.qty, 0))
  function add(product, qty = 1) { /* ... */ }
  return { items, count, add }
})
```

### 9.3 資料獲取

- SSR 資料用 `useApiData`（包裝 `useFetch`）；POST 互動用 `$api`（包裝 `$fetch`）。
- 勿在 setup 外部調用非同步生命週期鉤子。

### 9.4 樣式與主題

> [!IMPORTANT]
> **全站樣式規範**
> - 一律使用 Sass：所有 `<style>` 必須加 `lang="scss"`；嚴禁純 CSS、嚴禁無 `lang` 的 `<style>`，專案內不得出現獨立 `.css` 檔。
> - 嚴禁標籤行內 `style="..."`：所有樣式寫進 `<style lang="scss" scoped>` 以 class 套用；需動態切換時改用 `:class`。

- 主題色採 **CSS 變數**，定義於 `assets/styles/main.scss` 的 `:root`。
- 五種專案類型各有一組主題色，以 `[data-project='xxx']` 覆寫（`<html>` 屬性由 `app.vue` 設定）。
- 元件樣式用 `<style lang="scss" scoped>`，色彩一律引用 `var(--color-primary)` 等變數。

```scss
.btn { background: var(--color-primary); }
```

### 9.5 RWD 斷點 mixin

9 個斷點 mixin 定義於 `assets/styles/main.scss` 頂部，供撰寫全站共用的響應式樣式使用。

斷點（max-width）：`1920 / 1680 / 1440 / 1280 / 1200 / 1024 / 768 / 480 / 360`；其中 **1200** 為 header 變更樣式主斷點。

```scss
.box {
  width: 25%;
  @include rwd-1440 { width: 33%; }
  @include rwd-1200 { width: 50%; }
  @include rwd-768  { width: 100%; }
}
```

> mixin 僅 `main.scss` 內可 `@include`；元件 `<style lang="scss">` 的響應式樣式請直接寫 `@media (max-width: 1200px) { … }`。

### 9.6 路由中介層（middleware）

`middleware/` 放路由中介層 —— 進入頁面前執行的攔截 / 守衛邏輯（登入驗證、轉址、權限檢查等）。

- **具名中介層**：`middleware/auth.js`，於頁面以 `definePageMeta({ middleware: 'auth' })` 套用。
- **全域中介層**：檔名加 `.global` 後綴（`middleware/xxx.global.js`），每次路由切換皆自動執行。

```javascript
// middleware/auth.js — 具名中介層範例
export default defineNuxtRouteMiddleware((to, from) => {
  // 不符合條件時導向其他頁
  // if (!isLoggedIn()) return navigateTo('/login')
})
```

### 9.7 工具函式（utils）

`utils/` 放**純 JavaScript 工具函式**（格式化、驗證、計算等不涉及 Vue 響應式的邏輯）。Nuxt 會**自動引入**，全站免 `import` 直接呼叫。

- 與 `composables/` 的分工：`composables/` 放含響應式 / 生命週期的組合式函式（`useXxx`）；`utils/` 放無狀態的純函式。

```javascript
// utils/formatPrice.js — 工具函式範例
export function formatPrice(n) {
  return 'NT$ ' + Number(n).toLocaleString()
}
// 任何元件 / 頁面可直接呼叫 formatPrice(1280)，不需 import
```

---

## 10. 頁面與模組對照表

| 路由 | 檔案 | 對應 API（[一般網站白皮書](./一般網站白皮書.md)） |
|---|---|---|
| `/` | `pages/index.vue` | `/home/view`、`/seo/home`、`/banner/home` |
| `/news` | `pages/news/index.vue` | `/news/category`、`/news/list`、`/seo/list` |
| `/news/[slug]` | `pages/news/[slug].vue` | `/news/detail`、`/news/view` |
| `/about/[slug]` | `pages/about/[slug].vue` | `/about/detail`、`/about/view` |
| `/contact` | `pages/contact/index.vue` | `/contact/subjects`、`/contact/csrf`、`/contact/submit` |
| `/search` | `pages/search/index.vue` | `/search` |
| `/faq` | `pages/faq/index.vue` | `/faq/category`、`/faq/list` |
| `/location` | `pages/location/index.vue` | `/location/category`、`/location/list` |
| `/firm/caption` | `pages/firm/caption.vue` | 法規 / 政策頁 |
| `/shop` | `pages/shop/index.vue` | 購物模組（購物類型） |
| `/shop/[slug]` | `pages/shop/[slug].vue` | 商品詳細 |
| `/shop/cart` | `pages/shop/cart.vue` | 購物車（`useCartStore`） |
| 全站 | `AppHeader` / `AppFooter` / `PopupAds` | `/menu/view`、`/firm/view`、`/i18n`、`/popupsAd/list` |

---

## 11. Header 版型切換

頁首支援多版型，透過設定切換，不需改動版面程式碼（footer 等多版型區塊亦可比照辦理）。

### 11.1 檔案結構

```text
components/
 ┣ AppHeader.vue        派發器（依設定選版型，不含設計）
 ┗ headers/
    ┣ Header01.vue      版型 01（標準型：Logo 左、選單右）
    ┣ Header02.vue      版型 02（置中型：Logo 置中、選單於下方）
    ┗ HeaderXX.vue      …（依需求擴充，例如 15 種）
```

### 11.2 設定與切換

於 `.env` 指定要使用的版型編號：

```bash
# 選用 components/headers/ 內的版型編號
NUXT_PUBLIC_HEADER=01
```

切換版型只需改此值並重啟 dev server，版面程式碼完全不動。此設定與 `NUXT_PUBLIC_PROJECT_TYPE` 同屬 `runtimeConfig.public`，機制一致。

### 11.3 派發機制

`components/AppHeader.vue` 是薄派發器，以 Vite 的 `import.meta.glob` 自動載入 `headers/` 內全部版型，依 `NUXT_PUBLIC_HEADER` 選用對應檔；找不到時自動 fallback 第一個。

```javascript
const headers = import.meta.glob('./headers/*.vue', { eager: true })
const { public: pub } = useRuntimeConfig()
const current =
  headers[`./headers/Header${pub.header}.vue`]?.default ||
  Object.values(headers)[0]?.default
```

> ⚠️ 勿用 `resolveComponent('字串')` 取版型 —— Nuxt 自動引入元件無法以字串解析（同 `BlockRenderer` §8.2 的限制）。`import.meta.glob` 是正解。

### 11.4 新增版型

在 `headers/` 放一個 `HeaderXX.vue` 即可，`AppHeader.vue` 不需修改。各版型**共用同一套資料 composable**（`useSiteMenu` / `useProject` / `useUiStore`），只有版面與樣式不同 —— 資料層不重複撰寫。

---

## 修訂紀錄

| 版本 | 日期 | 內容 |
|---|---|---|
| v2.0 | 2026-05-22 | 調整類型規格：客製首頁／全客製／購物 的首頁改為「手刻 + block 模組組裝」、臨時站首頁定義為「固定模板」；`HomeCustom.vue` 加入 `BlockRenderer` 於手刻版型中嵌入 block 模組 |
| v2.0 | 2026-05-22 | 改版為母版專案白皮書：新增五種專案類型、類型切換機制、API 串接層、Mock JSON、Block 模組系統 |
| v1.0 | 2026-05-21 | 初版前端開發規範 |
